# Web App

This app lives in a workspace monorepo.

Use the root project README for full setup, seed logins, and Vercel deployment:

- `../../README.md`

Quick local run from repo root:

```bash
npm install
npm run dev
```
